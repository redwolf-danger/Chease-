{
    "name": "chease",
    "version": "1.0.0",
    "main": "index.js",
    "scripts": {
        "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
        "start": "npm run start --prefix backend",
        "prod": "npm run build && npm run start",
        "dev": "npm install --prefix backend && npm install --prefix frontend && npm run dev --prefix backend && npm run dev --prefix frontend",
        "test": "npm run dev --prefix frontend && npm run dev --prefix backend"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "description": "",
    "dependencies": {
        "chease": "file:"
    }
}